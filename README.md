# binary_serializer_deserializer_cpp
A simple C++ library for fast binary serialization and deserialization of data structures.

## compilation

because of the use in concepts, the compilation needs the -std=c++23 flag, like that:

```
g++ -std=c++23 ./examples/test.cpp ./src/* -o test
```
